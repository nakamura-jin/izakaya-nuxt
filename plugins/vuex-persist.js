// import VuexPersistence from 'vuex-persist'

// export default ({ store }) => {
//   new VuexPersistence({
//     storage: window.localStorage,
//     reducer: (state) => ({ cart: state.cart }),
//     filter: (mutation) => mutation.type === 'addToCart',
//   }).plugin(store)
// }